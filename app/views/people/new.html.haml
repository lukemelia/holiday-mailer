%h1 New person

- form_for(@person, :html => {:class => 'person_form'}) do |f|
  = f.error_messages

  #first_name= f.text_field :first_name
  #last_name= f.text_field :last_name
  #email= f.text_field :email
  
  =f.hidden_field :household_id

  %p= f.submit "Add person"

.back= link_to 'Cancel and go back', people_path
