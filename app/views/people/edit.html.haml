%h1 Editing person

= semantic_form_for(@person, :html => {:class => 'person_form'}) do |f|
  = errors_for(@person)

  = f.inputs do
    = f.input :first_name
    = f.input :last_name
    = f.input :email
  
  = f.buttons do
    = f.commit_button
  
= link_to 'Cancel and go back', people_path
